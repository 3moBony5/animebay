# دليل المساهمة - AnimeBay

شكراً لاهتمامك بالمساهمة في مشروع AnimeBay! نحن نرحب بجميع المساهمات سواء كانت تصحيحات أخطاء، مميزات جديدة، أو تحسينات على التوثيق.

## 🚀 كيفية البدء

### 1. استنساخ المشروع
```bash
git clone https://github.com/your-username/animebay.git
cd animebay
```

### 2. إنشاء فرع للتطوير
```bash
git checkout -b feature/your-feature-name
# أو
git checkout -b bugfix/issue-description
```

### 3. إعداد البيئة
- Android Studio Arctic Fox أو أحدث
- Android SDK 35
- Java 17
- Kotlin 2.0+

### 4. بناء المشروع
```bash
./gradlew build
```

## 📝 أنواع المساهمات

### 🐛 التبليغ عن الأخطاء (Bugs)
عند التبليغ عن خطأ، يُرجى تضمين:
- وصف واضح للمشكلة
- خطوات إعادة إنتاج الخطأ
- نسخة Android المستخدمة
- نموذج الجهاز
- لقطات شاشة (إن أمكن)
- سجلات الأخطاء (Logcat)

### 💡 اقتراح مميزات جديدة
عند اقتراح ميزة جديدة، يُرجى تضمين:
- وصف واضح للميزة
- الفائدة من هذه الميزة
- كيفية تنفيذها (إن أمكن)
- أمثلة أو لقطات شاشة من تطبيقات أخرى

### 🔧 طلبات السحب (Pull Requests)

#### قبل الإرسال
1. تأكد من أن الكود يعمل بشكل صحيح
2. اختبر على أجهزة مختلفة وإصدارات Android
3. تحقق من عدم وجود أخطاء في Logcat
4. تأكد من تنسيق الكود بشكل صحيح

#### معايير الكود
- اتباع [Kotlin Coding Conventions](https://kotlinlang.org/docs/coding-conventions.html)
- استخدام أسماء واضحة للمتغيرات والدوال
- إضافة تعليقات توضيحية للكود المعقد
- كتابة وثائق جافا (KDoc) للدوال العامة

#### رسالة الالتزام (Commit Message)
استخدم الصيغة التالية:
```
<النوع>(<النطاق>): <الوصف>

[الوصف التفصيلي]

[إغلاق القضايا]
```

**أنواع الالتزام:**
- `feat`: ميزة جديدة
- `fix`: إصلاح خطأ
- `docs`: تحديث في التوثيق
- `style`: تنسيق الكود فقط
- `refactor`: إعادة هيكلة الكود
- `test`: إضافة أو تحديث الاختبارات
- `chore`: تحديث المكتبات أو الأدوات

**أمثلة:**
```
feat(player): إضافة التحكم في سرعة التشغيل

- إضافة قائمة منسدلة لاختيار السرعة
- دعم السرعات من 0.5x إلى 2.0x
- حفظ السرعة المختارة

Closes #123
```

```
fix(search): إصلاح عدم ظهور عداد الحلقات

- إضافة دالة getEpisodes في AnimeRepository
- تحديث SearchViewModel لاستخراج عدد الحلقات
- تحديث UI لعرض العداد بشكل صحيح

Fixes #456
```

## 🏗️ بنية المشروع

```
app/src/main/java/io/animebay/stream/
├── data/
│   ├── model/          # نماذج البيانات
│   └── repository/     # مستودعات البيانات
├── ui/
│   ├── screens/        # الشاشات
│   └── theme/          # الثيم والتصميم
├── utils/              # الأدوات المساعدة
└── di/                 # الحقن التبعي
```

## 🧪 الاختبار

### اختبارات الوحدة (Unit Tests)
```bash
./gradlew test
```

### اختبارات Android
```bash
./gradlew connectedAndroidTest
```

### اختبارات UI
```bash
./gradlew assembleDebug
./gradlew assembleDebugAndroidTest
```

## 📋 قائمة المهام

### المميزات المطلوبة
- [ ] دعم الترجمة المدمجة
- [ ] نظام تعليقات وتقييمات
- [ ] إشعارات الحلقات الجديدة
- [ ] مدير تحميل متقدم
- [ ] إحصائيات المشاهدة

### الأخطاء المعروفة
- بعض السيرفرات قد لا تعمل في بعض الدول
- مشكلات في تحميل الصور على اتصال إنترنت بطيء
- بعض الأنمي قد لا يظهر عدد الحلقات بشكل صحيح

## 🎯 أولويات التطوير

1. **الاستقرار**: إصلاح الأخطاء الحرجة
2. **الأداء**: تحسين سرعة التطبيق
3. **المميزات**: إضافة مميزات جديدة مفيدة
4. **التوافق**: دعم المزيد من مواقع الأنمي

## 📞 التواصل

- **Issues**: استخدم GitHub Issues للتبليغ عن الأخطاء واقتراح المميزات
- **Discussions**: استخدم GitHub Discussions للأسئلة والمناقشات العامة
- **Email**: للمواضيع الحساسة

## 🏆 المساهمون

شكراً لكل من ساهم في هذا المشروع:

<!-- سيتم إضافة قائمة المساهمين تلقائياً -->

## 📄 الترخيص

هذا المشروع مخصص للتعلم والاستخدام الشخصي فقط. يُرجى احترام حقوق النشر الخاصة بمواقع الأنمي.

## 🙏 الشكر

شكراً لكل من:
- ساهم في كود المصدر المفتوح المستخدم
- قدم ملاحظات واقتراحات
- ساعد في اختبار التطبيق
- دعم المشروع

---

**سعداء بمساهمتكم! 🎉**
