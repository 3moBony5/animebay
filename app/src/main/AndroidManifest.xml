<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- إذن الوصول للإنترنت لجلب البيانات من الشبكة -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- إذن للوصول إلى حالة الشبكة، مطلوب لبعض مكتبات الشبكة المتقدمة -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- أذونات خاصة بتشغيل الخدمات في الواجهة (لأداة تسجيل الأخطاء) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- ✅ التعديل الأول: إضافة android:name هنا لتفعيل MyApplication -->
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnimeBay"
        android:usesCleartextTraffic="true"  
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AnimeBay"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ التعديل الثاني: إضافة تعريف ErrorActivity هنا -->
        <activity
            android:name=".utils.ErrorActivity"
            android:exported="false"
            android:theme="@style/Theme.AnimeBay" />

        <!-- تعريف خدمة تسجيل الأخطاء لمنع الانهيار -->
        <service
            android:name="com.itsaky.androidide.logsender.LogSenderService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />
            
    </application>

</manifest>
