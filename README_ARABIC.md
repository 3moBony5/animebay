# AnimeBay - محسّن بالكامل

تطبيق Android لبث وتحميل الأنمي مع دعم كامل لموقع WitAnime

---

## ✨ التحديثات الجديدة

### 1. دعم كامل لموقع WitAnime
- ✅ استخراج جميع سيرفرات المشاهدة (YonaPlay, StreamWish, Videa, Dailymotion)
- ✅ استخراج جميع روابط التحميل المباشرة (MediaFire, WorkUpload, Mp4Upload, GoFile)
- ✅ دعم جميع الجودات (SD, HD, FHD)

### 2. مشغل فيديو داخلي متقدم
- ✅ مشغل ExoPlayer احترافي
- ✅ دعم صيغ m3u8 و HLS
- ✅ دعم ملء الشاشة
- ✅ controls متقدمة (تشغيل/إيقاف، seek، تقدم/تراجع 10 ثواني)
- ✅ عرض الوقت والـ SeekBar

### 3. شاشة سيرفرات محسّنة
- ✅ عرض سيرفرات المشاهدة حسب الجودة
- ✅ عرض روابط التحميل المباشرة
- ✅ تبديل بين المشاهدة والتحميل
- ✅ أسماء مختصرة للسيرفرات

### 4. خوارزمية فك تشفير متقدمة
- ✅ فك تشفير Base64 البسيط
- ✅ فك التشفير المعقد (resourceRegistry و configRegistry)
- ✅ استخراج الروابط المخفية

---

## 🚀 البدء

### المتطلبات:
- Android Studio Hedgehog أو أحدث
- Android SDK 34
- Kotlin 1.9+

### التثبيت:

1. افتح المشروع في Android Studio
2. انتظر تحميل المكتبات
3. شغل التطبيق على جهاز أو محاكي

---

## 📱 الاستخدام

### 1. البحث عن الأنمي
- استخدم شاشة البحث للعثور على الأنمي المفضل
- اضغط على الأنمي لعرض التفاصيل

### 2. اختيار الحلقة
- من شاشة التفاصيل، اختر الحلقة التي تريد مشاهدتها
- اضغط على زر "المشاهدة" أو "التحميل"

### 3. اختيار السيرفر
- في شاشة السيرفرات، اختر السيرفر المناسب
- السيرفرات مرتبة حسب الجودة (FHD, HD, SD)
- يمكنك التبديل بين سيرفرات المشاهدة وروابط التحميل

### 4. المشاهدة
- في شاشة المشغل، استخدم controls للتحكم في الفيديو
- اضغط مرتين على الشاشة للدخول/الخروج من ملء الشاشة
- استخدم أزرار تقدم/تراجع 10 ثواني للتنقل السريع

---

## 🔧 كيف يعمل

### استخراج السيرفرات:

1. **البحث عن السكريبتات**
   - البحث عن `var _m =` للحصول على مفتاح التشفير
   - البحث عن `var _a =` للحصول على مصفوفة السيرفرات

2. **فك التشفير**
   - فك تشفير Base64 للمفتاح
   - تطبيق XOR بين الروابط المشفرة والمفتاح
   - فك تشفير Base64 للنتيجة

3. **التشفير المعقد**
   - البحث عن `_zG` و `_zH` في السكريبتات
   - فك تشفير Base64 للبيانات
   - تحليل JSON
   - تطبيق خوارزمية فك التشفير المخصصة

### خوارزمية فك التشفير:

```kotlin
// 1. فك تشفير المفتاح
val decodedKey = String(Base64.decode(key, Base64.DEFAULT))

// 2. تطبيق XOR
var result = ""
for (i in encoded.indices) {
    val encodedChar = encoded[i]
    val keyChar = decodedKey[i % decodedKey.length]
    result += (encodedChar.code xor keyChar.code).toChar()
}

// 3. فك تشفير النتيجة
val finalUrl = String(Base64.decode(result, Base64.DEFAULT))
```

---

## 📊 السيرفرات المدعومة

### سيرفرات المشاهدة:
| السيرفر | الجودة | الحالة |
|---------|--------|--------|
| YonaPlay | Multi | ✅ |
| StreamWish | SD, FHD | ✅ |
| Videa | FHD | ✅ |
| Dailymotion | FHD | ✅ |

### مضيفي التحميل:
| المضيف | الجودة | الحالة |
|--------|--------|--------|
| MediaFire | SD, HD, FHD | ✅ |
| WorkUpload | SD, HD, FHD | ✅ |
| Mp4Upload | SD, HD, FHD | ✅ |
| GoFile | SD, HD, FHD | ✅ |

---

## 🎮 المميزات الجديدة

### 1. مشغل فيديو احترافي
- ✅ ExoPlayer بدلاً من WebView
- ✅ دعم ملء الشاشة الأفقية
- ✅ controls سهلة الاستخدام
- ✅ عرض الوقت والمدة

### 2. استخراج شامل
- ✅ جميع سيرفرات المشاهدة
- ✅ جميع روابط التحميل
- ✅ جميع الجودات المتاحة

### 3. تجربة مستخدم محسّنة
- ✅ شاشة سيرفرات منظمة حسب الجودة
- ✅ تبديل سهل بين المشاهدة والتحميل
- ✅ أسماء مختصرة للسيرفرات

---

## 🔍 كود مهم

### دالة فك التشفير:
```kotlin
// في AnimeRepository.kt
private fun decodeServerUrl(encoded: String, key: String): String {
    val decodedKey = String(Base64.decode(key, Base64.DEFAULT))
    var result = ""
    for (i in encoded.indices) {
        result += (encoded[i].code xor decodedKey[i % decodedKey.length].code).toChar()
    }
    return String(Base64.decode(result, Base64.DEFAULT))
}
```

### دالة استخراج السيرفرات:
```kotlin
// في ServersViewModel.kt
suspend fun getEpisodeServers(episodeUrl: String): List<Pair<String, String>> {
    // البحث عن السكريبتات
    // فك التشفير
    // إرجاع قائمة السيرفرات
}
```

---

## 🛠️ التخصيص

### إضافة سيرفر جديد:

1. أضف اسم السيرفر في `getDisplayName()`
2. أضف منطق فك التشفير إذا كان مختلفاً
3. أضف معالجة خاصة في `PlayerScreen.kt`

### تغيير جودة الفيديو:

```kotlin
// في ServersViewModel.kt
private fun getQualityFromName(name: String): String {
    return when {
        name.contains("4K", ignoreCase = true) -> "2160p - 4K"
        name.contains("FHD", ignoreCase = true) -> "1080p - FHD"
        name.contains("HD", ignoreCase = true) -> "720p - HD"
        else -> "480p - SD"
    }
}
```

---

## 🐛 حل المشاكل

### المشكلة: لا يتم عرض السيرفرات
**الحل:**
- تأكد من اتصال الإنترنت
- تأكد من صحة رابط الحلقة
- افحص الـ Logcat للأخطاء

### المشكلة: الفيديو لا يعمل
**الحل:**
- جرب سيرفر آخر
- تأكد من دعم صيغة m3u8
- افحص الـ Logcat للأخطاء

### المشكلة: التطبيق يتوقف
**الحل:**
- افحص الـ Logcat للاستثناءات
- تأكد من صحة البيانات المستخرجة
- تحقق من صحة JSON

---

## 📈 الأداء

### تحسينات:
- ✅ استخدام OkHttp لطلبات الشبكة
- ✅ استخدام Coroutines للمعالجة غير المتزامنة
- ✅ تخزين مؤقت للبيانات
- ✅ إزالة الروابط المكررة

### النصائح:
- استخدم Wi-Fi للحصول على أفضل أداء
- أغلق التطبيقات الأخرى لتوفير الذاكرة
- حدّث التطبيق باستمرار للحصول على أحدث التحسينات

---

## 🔄 التحديثات المستقبلية

### المخطط:
- ✅ إضافة المزيد من السيرفرات
- ✅ تحسين خوارزمية فك التشفير
- ✅ إضافة قائمة المفضلة
- ✅ إضافة تاريخ المشاهدة
- ✅ تحميل الحلقات للمشاهدة دون اتصال

---

## 📞 الدعم

### للمساعدة:
1. افحص الـ Logcat للأخطاء
2. راجع ملفات README
3. تحقق من صحة الروابط
4. جرب السيرفرات المختلفة

---

## 📄 ملاحظات

### الترخيص:
هذا التطبيق للاستخدام الشخصي فقط.

### المسؤولية:
- احترم حقوق الملكية الفكرية
- استخدم التطبيق بمسؤولية
- لا تشارك الروابط بشكل غير قانوني

---

## 🎉 الاستمتاع

استمتع بمشاهدة الأنمي المفضل لديك مع أفضل تجربة ممكنة!

---

**تم التحديث:** 2026-01-18  
**الإصدار:** 2.0  
**المطور:** محسّن بالكامل
